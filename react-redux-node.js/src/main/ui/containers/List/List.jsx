import React from 'react';
import styles from './List.scss';

const List = ({ }) => (
    <div className={styles.wrapper}>
        <p className="">
            HI
        </p>
    </div>
);

export default List;
